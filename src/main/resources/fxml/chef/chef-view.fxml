<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>

<StackPane xmlns="http://javafx.com/javafx/20"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="ilcaminodelamamma.view.chef.ChefViewController"
           stylesheets="@../../css/chef-view.css"
           fx:id="rootStackPane">
           
<BorderPane fx:id="mainBorderPane"
            minWidth="900" minHeight="600">

    <!-- Barra Superior -->
    <top>
        <HBox styleClass="top-bar" alignment="CENTER_LEFT" spacing="20">
            <padding>
                <Insets top="12" bottom="12" left="20" right="20"/>
            </padding>
            
            <!-- Logo más grande -->
            <ImageView fitWidth="80" fitHeight="80" preserveRatio="true">
                <image>
                    <Image url="@../../img/logo.png"/>
                </image>
            </ImageView>
            
            <Label text="Il Camino Della Mamma" style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #2C1810;"/>
            
            <Region HBox.hgrow="ALWAYS"/>
            
            <!-- Buscador responsive -->
            <TextField fx:id="searchField" promptText="🔍 Buscar recetas..." 
                       minWidth="200" prefWidth="350" maxWidth="450" styleClass="search-field"
                       HBox.hgrow="SOMETIMES"/>
        </HBox>
    </top>

    <!-- Panel Lateral Izquierdo (Sidebar) - Responsive -->
    <left>
        <VBox styleClass="sidebar" minWidth="180" prefWidth="220" maxWidth="280">
            <padding>
                <Insets top="25" bottom="20" left="15" right="15"/>
            </padding>
            
            <!-- Título del menú con separador -->
            <VBox spacing="10" styleClass="menu-header">
                <Label text="MENÚ" styleClass="sidebar-title"/>
                <Separator styleClass="menu-separator"/>
            </VBox>
            
            <VBox.margin>
                <Insets bottom="20"/>
            </VBox.margin>
            
            <!-- Botones del menú -->
            <VBox spacing="8" VBox.vgrow="ALWAYS">
                <Button fx:id="btnNuevaReceta" text="➕ Nueva Receta" maxWidth="Infinity" 
                        styleClass="sidebar-button,primary-button"/>
                
                <Region prefHeight="10"/>
                
                <Button fx:id="btnLibros" text="📚 Libros de cocina" maxWidth="Infinity"
                        styleClass="sidebar-button"/>
                        
                <Button fx:id="btnComandas" text="📋 Lista de Comandas" maxWidth="Infinity"
                        styleClass="sidebar-button"/>
                
                <Button fx:id="btnComandasTerminadas" text="✅ Comandas Terminadas" maxWidth="Infinity"
                        styleClass="sidebar-button"/>
                        
                <Button fx:id="btnIngredientes" text="🥬 Ingredientes" maxWidth="Infinity"
                        styleClass="sidebar-button"/>
                
                <Region VBox.vgrow="ALWAYS"/>
                
                <!-- Sección inferior -->
                <Separator styleClass="menu-separator"/>
                
                <Button fx:id="btnConfiguracion" text="⚙️ Configuración" maxWidth="Infinity"
                        styleClass="sidebar-button"/>
                        
                <Button fx:id="btnCerrarSesion" text="🚪 Cerrar Sesión" maxWidth="Infinity"
                        styleClass="sidebar-button,logout-button"/>
            </VBox>
        </VBox>
    </left>

    <!-- Área Central -->
    <center>
        <VBox fx:id="centerArea" styleClass="center-area" spacing="15">
            <padding>
                <Insets top="20" bottom="20" left="20" right="20"/>
            </padding>
            
            <!-- Breadcrumb de navegación -->
            <HBox fx:id="breadcrumbBox" spacing="5" alignment="CENTER_LEFT" 
                  visible="false" managed="false">
                <padding>
                    <Insets bottom="5"/>
                </padding>
            </HBox>
            
            <!-- Título -->
            <Label fx:id="contentTitle" text="Todas las recetas (300)" styleClass="content-title"/>
            
            <!-- Pestañas de filtro -->
            <HBox spacing="20" styleClass="tab-bar">
                <Button fx:id="tabPlatos" text="Platos" styleClass="tab-button,active"/>
                <Button fx:id="tabCategorias" text="Categorías" styleClass="tab-button"/>
                <Button fx:id="tabFavoritos" text="Favoritos" styleClass="tab-button"/>
            </HBox>
            
            <!-- Grid de categorías/platos con scroll -->
            <ScrollPane fx:id="mainScrollPane" fitToWidth="true" fitToHeight="true" VBox.vgrow="ALWAYS" 
                       styleClass="scroll-pane" hbarPolicy="NEVER">
                <GridPane fx:id="categoryGrid" styleClass="category-grid" 
                         hgap="20" vgap="20" alignment="CENTER">
                    <padding>
                        <Insets top="10" bottom="10" left="10" right="10"/>
                    </padding>
                </GridPane>
            </ScrollPane>
        </VBox>
    </center>

    <!-- Panel Derecho - Responsive -->
    <right>
        <VBox style="-fx-background-color: #E8D4B8; -fx-padding: 15; -fx-spacing: 15;" 
              minWidth="180" prefWidth="250" maxWidth="300">
            <Label text="Vistos recientemente" 
                   style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #2C1810;"/>
            
            <!-- Scroll para recetas recientes -->
            <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS" 
                       style="-fx-background-color: transparent; -fx-background: transparent;"
                       hbarPolicy="NEVER">
                <VBox fx:id="recentRecipesPanel" spacing="10" 
                      style="-fx-background-color: transparent;"/>
            </ScrollPane>
        </VBox>
    </right>

    <!-- Barra Inferior -->
    <bottom>
        <HBox styleClass="bottom-bar">
            <padding>
                <Insets top="15" bottom="15" left="15" right="15"/>
            </padding>
            <Label text="Sistema de gestión de recetas - Il Camino Della Mamma" styleClass="bottom-text"/>
        </HBox>
    </bottom>

</BorderPane>

<!-- Panel de overlay para detalles de receta (inicialmente oculto) -->
<VBox fx:id="recipeDetailOverlay" visible="false" managed="false"
      style="-fx-background-color: rgba(0,0,0,0.5);" alignment="CENTER">
    <StackPane fx:id="recipeDetailContainer" maxWidth="900" maxHeight="700"
               style="-fx-background-color: #F5F1ED; -fx-background-radius: 15;">
    </StackPane>
</VBox>

</StackPane>
